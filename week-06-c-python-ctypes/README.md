# Week 6 — Calling C from Python

Dry run for calling compiled C code from Python using `ctypes`, in
preparation for interfacing with `nuLASCL` (the NUCES OpenCL-based linear
algebra library) from a Python/Spark workflow.

See `ctypes-vector-add/` for the task.
